{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "windows: build installer (Setup.exe)",
            "detail": "Publishes Wallpache self-contained win-x64 and compiles WallpacheSetup-<version>.exe into dist/. Use this instead of C# Dev Kit's Publish command, which manages its own profile and won't necessarily use FolderProfile.pubxml.",
            "type": "shell",
            "command": "powershell",
            "args": [
                "-NoProfile",
                "-ExecutionPolicy",
                "Bypass",
                "-File",
                "${workspaceFolder}/scripts/make-installer.ps1"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
