# Wallpache

Wallpache is a multi-platform live wallpaper project.

## Applications

- `apps/macos` — native macOS application
- `apps/windows` — Windows application
- `apps/mobile` — mobile application

Shared assets and contracts live in `shared`, while implementation plans and
other project documentation live in `docs`.
